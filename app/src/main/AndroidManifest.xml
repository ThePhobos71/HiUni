<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.NFC" />

    <!-- NFC nicht erzwingen: Geräte ohne Chip dürfen die App trotzdem nutzen,
         die Mensa-Karten-Funktion ist dort nur ausgegraut. -->
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <application
        android:name=".HiUniApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HiUni.Splash"
        tools:targetApi="34">

        <!--
            MainActivity selbst hat KEINEN MAIN/LAUNCHER-Intent-Filter mehr.
            Der Launcher-Einstieg läuft ausschließlich über die activity-alias-
            Einträge weiter unten — genau einer ist `enabled=true`, die anderen
            sind `enabled=false`. Der AppIconManager toggelt das zur Laufzeit
            via PackageManager.setComponentEnabledSetting(), so dass der User
            in den Settings zwischen Icon-Varianten umschalten kann.

            Der NFC-Intent-Filter (TECH_DISCOVERED) bleibt direkt an der
            Activity — er hat nichts mit dem Launcher-Icon zu tun und soll
            unabhängig vom gewählten Icon immer triggern. Würde er an einem
            Alias hängen, bräuchte jeder Alias eine eigene Kopie inklusive
            Meta-Data-Resource.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.HiUni.Splash">
            <!-- App-Launch direkt an die DESfire-Karte (IsoDep). Mit dem
                 Tech-Filter in res/xml/nfc_tech_filter.xml triggert das
                 ausschließlich für ISO-14443-4-Karten, nicht für jeden
                 NFC-Tag. Foreground-Dispatch übersteuert das, solange die
                 App schon offen ist. -->
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />
        </activity>

        <!--
            Custom-App-Icon-Switcher via Activity-Aliases (iOS/WhatsApp/Telegram-
            Premium-Pattern). Jeder Alias trägt sein eigenes android:icon /
            android:roundIcon plus den MAIN/LAUNCHER-Intent-Filter, und genau
            einer ist initial enabled (Default). Die anderen werden via
            PackageManager.setComponentEnabledSetting() zur Laufzeit aktiviert/
            deaktiviert (siehe core/icon/AppIconManager.kt).

            Wichtig: targetActivity zeigt immer auf dieselbe MainActivity —
            der Alias ist nur eine zusätzliche Component-Identität für den
            Launcher. Würden mehrere Aliases gleichzeitig enabled sein, hätte
            der User mehrere App-Icons im Launcher; deshalb garantiert der
            AppIconManager das "exactly one"-Invariant beim Switch.
        -->
        <activity-alias
            android:name=".MainActivity.IconDefault"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:label="@string/app_name"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivity.IconDark"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_dark"
            android:roundIcon="@mipmap/ic_launcher_dark_round"
            android:label="@string/app_name"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivity.IconClassic"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_classic"
            android:roundIcon="@mipmap/ic_launcher_classic_round"
            android:label="@string/app_name"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivity.IconStudi"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_studi"
            android:roundIcon="@mipmap/ic_launcher_studi_round"
            android:label="@string/app_name"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".core.auth.WebLoginActivity"
            android:exported="false"
            android:label="Uni-Login"
            android:theme="@style/Theme.HiUni" />

        <receiver
            android:name=".core.notifications.NotificationReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
            WorkManager-Default-Init via androidx.startup deaktivieren: ohne dieses
            tools:node="remove" startet WorkManager VOR Application.onCreate mit der
            Default-WorkerFactory und ignoriert anschließend unsere @HiltWorker-
            Konfiguration aus HiUniApplication.workManagerConfiguration. Resultat
            wäre NoSuchMethodException beim Instanziieren von LsfSyncWorker (kein
            (Context, WorkerParameters)-Konstruktor, weil @AssistedInject).
            Mit dem Remove übernimmt das On-Demand-Init via Configuration.Provider,
            das die HiltWorkerFactory kennt.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
