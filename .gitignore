# Gradle
.gradle/
build/
app/build/
/build
/captures
/local.properties
local.properties

# IDE — Android Studio / IntelliJ
*.iml
.idea/

# Kotlin
.kotlin/

# Native build
.externalNativeBuild
.cxx

# Output / Artefakte
/dist/
*.apk
*.aab

# OS
.DS_Store
Thumbs.db

# Logs / scratch
*.log
*.tmp
*.swp

# Firebase / Secrets — niemals committen
app/google-services.json
*service-account*.json
server/.env
server/secrets/
__pycache__/
*.pyc

# Release-Signing — Keystore und Passwörter bleiben lokal (siehe docs/RELEASE_SIGNING.md)
*.jks
*.keystore
keystore.properties

# Room schemas WERDEN committed (Migration-Snapshots) — explizit nicht ignoriert
# !app/schemas/
presentation/
.claude/
